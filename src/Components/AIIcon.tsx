/* import React from "react"

import AIcon from "../../public/AIcon.svg" // Adjusted import path

interface AIIconProps {
  onClick: () => void
}

const AIIcon: React.FC<AIIconProps> = ({ onClick }) => {
  return (
    <div
      className="fixed bottom-4 right-4 cursor-pointer z-50"
      onClick={onClick}>
      <img src={AIcon} alt="AI Icon" className="w-12 h-12" />
    </div>
  )
}

export default AIIcon
 */
